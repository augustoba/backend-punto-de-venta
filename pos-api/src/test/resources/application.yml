spring:
  datasource:
    url: jdbc:h2:mem:pos;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
pos:
  cors-origins: http://localhost:4200
